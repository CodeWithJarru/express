<!DOCTYPE html>
<html lang="en">

<head>
  <title>CodersBlog</title>
  <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  <style>
    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-thumb {
      background: #555;
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #888;
    }
  </style>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap');

    body,
    html {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow-x: hidden;
      background-color: white;
      color: black;
      font-family: "Poppins", sans-serif;
    }
    .container {
      padding: 22px;
    }

    .hero {
      display: flex;
      flex-direction: row;
      align-items: center;
      /* flex-wrap: wrap; */
      justify-content: center;
      gap: 30px;
      height: 100vh;
      text-align: center;
      background-color: #111825;
      z-index: 9;
    }

    .hero-svg {
      width: 20vh;
      height: 20vh;
      opacity: 0.1;
      z-index: 9;
      animation-name: rotate;
      animation-duration: 20s;
      animation-iteration-count: infinite;
      animation-timing-function: ease-in-out;
      animation-delay: 0s;
      animation-play-state: running;
    }
    @keyframes rotate {
      0% {
        transform: rotate(0deg);
      }

      100% {
        transform: rotate(360deg);
      }
    }

    .hero h1 {
      font-size: 50px;
      margin-bottom: 20px;
      color: white;
      z-index: 10;
    }
    .hero p {
      font-size: 20px;
      margin-bottom: 20px;
      color: white;
      z-index: 10;
    }

    .about {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      gap: 15px;
      height: 100vh;
      flex-wrap: wrap;
      text-align: center;
    }

    .about h1 {
      font-size: 50px;
      margin-bottom: 20px;
      color: #111825;
    }

    .container .title {
      font-size: 40px;
    }

    .navbar {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 12px;
      padding: 12px 0;
      background-color: #111825;
      color: white;
      align-items: center;
    }

    .navbar-brand {
      font-size: 24px;
      font-weight: 600;
      color: white;
      text-decoration: none;
    }

    .navbar ul {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
    }

    .nav-item {
      color: white;
      list-style-type: none;
    }

    .nav-link {
      color: white;
      text-decoration: none;
    }

    .blog-container {
      padding: 12px;
      /* height: 50vh; */
      width: 98%;
      gap: 5px;
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
    }

    .blog-slide {
      scroll-snap-align: start;
      /* height: 40vh; */
      padding: 2rem;
      gap: 12px;
      display: flex;
      justify-content: center;
      flex-direction: row;
      flex-wrap: wrap;
      align-items: center;
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .blog-slide.active {
      opacity: 1;
      transform: translateY(0);
      z-index: 1;
    }

    .blog-card {
      max-width: 500px;
      width: 400px;
      padding: 40px;
      background: transparent;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.4);
    }

    .blog-title {
      font-size: 2rem;
      margin-bottom: 15px;
    }

    .blog-content {
      font-size: 1.1rem;
      margin-bottom: 15px;
    }

    .blog-author {
      font-size: 0.9rem;
      color: #aaa;
      margin-bottom: 15px;
    }

    .read-more {
      display: inline-block;
      padding: 10px 20px;
      background-color: black;
      color: #fff;
      border-radius: 5px;
      text-decoration: none;
      font-weight: bold;
      transition: background 0.3s ease;
    }

    .code {
  background-color: #1e1e1e;
  color: #dcdcdc;
  padding: 25px;
  font-size: 12px;
  white-space: wrap;
  border-radius: 8px;
  font-family: monospace;
  margin: 10px 0;
  overflow-x: auto;
}

.post {
  box-shadow: 0 0 3px 1px rgba(0, 0, 0, 0.4);
  border-radius: 12px;
  padding: 1px 40px;
}

    .read-more:hover {
      background-color: #212121;
    }



    .view-more {
      display: inline-block;
      margin-top: 10px;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .want-more-blogs a:hover {
      background-color: #0056b3;
    }
  </style>
</head>

<body>

  <!-- Navbar with Dropdown -->
  <nav class="navbar">
    <a class="navbar-brand" href="/">CodersBlog</a>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/about">About</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/contact">Contact</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="/posts" role="button" data-bs-toggle="dropdown">
            Posts
          </a>

        </li>
      </ul>
    </div>
  </nav>